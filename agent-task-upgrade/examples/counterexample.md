# Контрпример

Вход:

```text
Добавь кеш для отчётов.
```

Недопустимо сразу формировать цель «внедрить Redis-кеш с TTL 10 минут», потому что
технология, TTL, стратегия инвалидирования и поведение при отказе отсутствуют во
входе. Корректный результат — `NEEDS_INPUT`.
